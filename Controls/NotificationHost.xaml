<UserControl
    x:Class="GestionTime.Desktop.Controls.NotificationHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- 🔍 DEBUG ULTRA SIMPLE - VERSIÓN MINIMALISTA PARA DIAGNÓSTICO -->
    <Grid Background="Red" 
          Width="400" 
          Height="300" 
          HorizontalAlignment="Right" 
          VerticalAlignment="Bottom"
          Margin="20">
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="🔔 NOTIFICATIONHOST ACTIVO" 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="Bold"
                       TextAlignment="Center"/>
            
            <TextBlock Text="{x:Bind ViewModel.ActiveNotifications.Count, Mode=OneWay}" 
                       Foreground="Yellow" 
                       FontSize="48" 
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0,20,0,0"/>
            
            <TextBlock Text="notificaciones activas" 
                       Foreground="White" 
                       FontSize="16"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
