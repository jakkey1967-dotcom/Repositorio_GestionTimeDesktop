<Page
    x:Class="GestionTime.Desktop.Views.ParteItemEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:GestionTime.Desktop.Helpers"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    mc:Ignorable="d"
    Background="Transparent">

    <!-- ===================== RESOURCES - PALETA EXACTA PANTALLAZO ===================== -->
    <Page.Resources>
        <ResourceDictionary>
            <!-- 🎨 PALETA DE COLORES EXACTA DEL PANTALLAZO -->

            
            <!-- 📝 ESTILO TEXTBOX -->
            <Style x:Key="ModernTextBox" TargetType="TextBox">
                <Setter Property="Background" Value="{ThemeResource AppInputBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource AppTextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Height" Value="38"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            
            <!-- 📝 ESTILO AUTOSUGGESTBOX -->
            <Style x:Key="ModernAutoSuggestBox" TargetType="AutoSuggestBox">
                <Setter Property="Background" Value="{ThemeResource AppInputBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource AppTextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Height" Value="38"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextMemberPath" Value=""/>
                <Setter Property="TextBoxStyle">
                    <Setter.Value>
                        <Style TargetType="TextBox">
                            <Setter Property="TextAlignment" Value="Left"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- 📅 ESTILO DATEPICKER -->
            <Style x:Key="ModernDatePicker" TargetType="CalendarDatePicker">
                <Setter Property="Background" Value="{ThemeResource AppInputBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource AppTextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Height" Value="38"/>
            </Style>
            
            <!-- 🔽 ESTILO COMBOBOX -->
            <Style x:Key="ModernComboBox" TargetType="ComboBox">
                <Setter Property="Background" Value="{ThemeResource AppInputBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource AppTextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Height" Value="38"/>
            </Style>
            
            <!-- 🔘 BOTÓN PRIMARIO (GRABAR) -->
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource AppPrimaryBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            
            <!-- 🔘 BOTÓN SECUNDARIO (ANULAR/SALIR) -->
            <Style x:Key="SecondaryButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{ThemeResource AppTextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            
            <!-- 📌 LABEL CAMPO -->
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource AppTextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Normal"/>
                <Setter Property="Margin" Value="0,0,0,4"/>
            </Style>
            
            <!-- 🏷️ SECTION TITLE -->
            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource AppPrimaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>
        </ResourceDictionary>
    </Page.Resources>

    <!-- ===================== MAIN GRID RESPONSIVE CON FONDO OSCURO ===================== -->
    <Grid x:Name="RootGrid" Padding="8" Opacity="0">
        <!-- FONDO OSCURO SÓLIDO -->
        <Grid.Background>
            <SolidColorBrush Color="{ThemeResource AppBackgroundBrush}"/>
        </Grid.Background>
        
        <!-- // VISUALSTATEMANAGER - Breakpoints responsive -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowSizeStates">
                <!-- // Small: <920px - Una columna, todo apilado -->
                <VisualState x:Name="Small">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Card 1: Datos Generales - 1 columna -->
                        <Setter Target="Card1Grid.ColumnDefinitions[0].Width" Value="*"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[1].Width" Value="0"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[2].Width" Value="0"/>
                        <Setter Target="ClienteStack.(Grid.Column)" Value="0"/>
                        <Setter Target="ClienteStack.(Grid.Row)" Value="1"/>
                        <Setter Target="TiendaStack.(Grid.Column)" Value="0"/>
                        <Setter Target="TiendaStack.(Grid.Row)" Value="2"/>
                        
                        <!-- Card 2: Información de Tiempo - 1 columna -->
                        <Setter Target="Card2Grid.ColumnDefinitions[0].Width" Value="*"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[1].Width" Value="0"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[2].Width" Value="0"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[3].Width" Value="0"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[4].Width" Value="0"/>
                        <Setter Target="HoraFinStack.(Grid.Column)" Value="0"/>
                        <Setter Target="HoraFinStack.(Grid.Row)" Value="1"/>
                        <Setter Target="HoraIniStack.Margin" Value="0,0,0,8"/>
                        <Setter Target="HoraFinStack.Margin" Value="0,0,0,8"/>
                        <Setter Target="TicketStack.(Grid.Column)" Value="0"/>
                        <Setter Target="TicketStack.(Grid.Row)" Value="2"/>
                        <Setter Target="TicketStack.Margin" Value="0,0,0,8"/>
                        <Setter Target="GrupoStack.(Grid.Column)" Value="0"/>
                        <Setter Target="GrupoStack.(Grid.Row)" Value="3"/>
                        <Setter Target="GrupoStack.Margin" Value="0,0,0,8"/>
                        <Setter Target="TipoStack.(Grid.Column)" Value="0"/>
                        <Setter Target="TipoStack.(Grid.Row)" Value="4"/>
                        <Setter Target="TipoStack.Margin" Value="0,0,0,8"/>
                        <Setter Target="PnlDayTimeHistory.(Grid.Row)" Value="5"/>

                        <!-- Botones footer - apilados verticalmente -->
                        <Setter Target="FooterButtons.Orientation" Value="Vertical"/>
                        <Setter Target="FooterButtons.HorizontalAlignment" Value="Stretch"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- // Medium: >=920px && <1200px - Híbrido OPTIMIZADO -->
                <VisualState x:Name="Medium">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="920" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Card 1: 3 columnas compactas (Fecha | Cliente | Tienda) -->
                        <Setter Target="Card1Grid.ColumnDefinitions[0].Width" Value="150"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[1].Width" Value="*"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[2].Width" Value="200"/>
                        <Setter Target="ClienteStack.(Grid.Column)" Value="1"/>
                        <Setter Target="ClienteStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TiendaStack.(Grid.Column)" Value="2"/>
                        <Setter Target="TiendaStack.(Grid.Row)" Value="0"/>
                        
                        <!-- Card 2: 4 columnas en 2 filas (Horas+Ticket | Grupo+Tipo) -->
                        <Setter Target="Card2Grid.ColumnDefinitions[0].Width" Value="110"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[1].Width" Value="110"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[2].Width" Value="170"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[3].Width" Value="*"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[4].Width" Value="*"/>
                        <Setter Target="HoraIniStack.Margin" Value="0"/>
                        <Setter Target="HoraFinStack.(Grid.Column)" Value="1"/>
                        <Setter Target="HoraFinStack.(Grid.Row)" Value="0"/>
                        <Setter Target="HoraFinStack.Margin" Value="0"/>
                        <Setter Target="TicketStack.(Grid.Column)" Value="2"/>
                        <Setter Target="TicketStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TicketStack.Margin" Value="0"/>
                        <Setter Target="GrupoStack.(Grid.Column)" Value="3"/>
                        <Setter Target="GrupoStack.(Grid.Row)" Value="0"/>
                        <Setter Target="GrupoStack.Margin" Value="0"/>
                        <Setter Target="TipoStack.(Grid.Column)" Value="4"/>
                        <Setter Target="TipoStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TipoStack.Margin" Value="0"/>
                        <Setter Target="PnlDayTimeHistory.(Grid.Row)" Value="1"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- // Large: >=1200px - Layout completo horizontal -->
                <VisualState x:Name="Large">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Card 1: 3 columnas (Fecha | Cliente | Tienda) -->
                        <Setter Target="Card1Grid.ColumnDefinitions[0].Width" Value="150"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[1].Width" Value="*"/>
                        <Setter Target="Card1Grid.ColumnDefinitions[2].Width" Value="220"/>
                        <Setter Target="ClienteStack.(Grid.Column)" Value="1"/>
                        <Setter Target="ClienteStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TiendaStack.(Grid.Column)" Value="2"/>
                        <Setter Target="TiendaStack.(Grid.Row)" Value="0"/>
                        
                        <!-- Card 2: 5 columnas (todas en una fila) -->
                        <Setter Target="Card2Grid.ColumnDefinitions[0].Width" Value="100"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[1].Width" Value="100"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[2].Width" Value="150"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[3].Width" Value="*"/>
                        <Setter Target="Card2Grid.ColumnDefinitions[4].Width" Value="*"/>
                        <Setter Target="HoraIniStack.Margin" Value="0"/>
                        <Setter Target="HoraFinStack.(Grid.Column)" Value="1"/>
                        <Setter Target="HoraFinStack.(Grid.Row)" Value="0"/>
                        <Setter Target="HoraFinStack.Margin" Value="0"/>
                        <Setter Target="TicketStack.(Grid.Column)" Value="2"/>
                        <Setter Target="TicketStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TicketStack.Margin" Value="0"/>
                        <Setter Target="GrupoStack.(Grid.Column)" Value="3"/>
                        <Setter Target="GrupoStack.(Grid.Row)" Value="0"/>
                        <Setter Target="GrupoStack.Margin" Value="0"/>
                        <Setter Target="TipoStack.(Grid.Column)" Value="4"/>
                        <Setter Target="TipoStack.(Grid.Row)" Value="0"/>
                        <Setter Target="TipoStack.Margin" Value="0"/>
                        <Setter Target="PnlDayTimeHistory.(Grid.Row)" Value="1"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <!-- // Grid principal con 3 filas: Header | Content | Footer -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Header -->
            <RowDefinition Height="*"/>      <!-- Content (ScrollViewer) -->
            <RowDefinition Height="Auto"/>   <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- ===================== HEADER - BANNER TEAL (COMPACTO) ===================== -->
        <Border Grid.Row="0" 
                Background="{ThemeResource AppPrimaryBrush}"
                CornerRadius="6" 
                Padding="16">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- LOGO con esquinas redondeadas (igual que DiarioPage) -->
                <Border Grid.Column="0" 
                        Background="Transparent" 
                        CornerRadius="6" 
                        VerticalAlignment="Center">
                    <Image x:Name="LogoImageBanner"
                           Source="ms-appx:///Assets/LogoOscuro.png"
                           Stretch="Uniform"
                           MaxHeight="60"
                           HorizontalAlignment="Left"/>
                </Border>
                
                <!-- TÍTULO + USUARIO (mismo tamaño que DiarioPage) -->
                <StackPanel Grid.Column="1" 
                            VerticalAlignment="Center" 
                            Spacing="4"
                            Margin="8,0,0,0">
                    <!-- Título -->
                    <TextBlock x:Name="TxtTituloParte" 
                               Text="Nuevo Parte"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                    
                    <!-- Usuario con información dinámica (igual que DiarioPage) -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE77B;" 
                                  FontSize="14" 
                                  Foreground="White" 
                                  Opacity="0.9"
                                  VerticalAlignment="Center"/>
                        <StackPanel Spacing="2">
                            <!-- Nombre completo dinámico -->
                            <TextBlock x:Name="TxtUserName" 
                                       Text="Usuario"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="White"/>
                            
                            <!-- Email dinámico -->
                            <TextBlock x:Name="TxtUserEmail" 
                                       Text="usuario@empresa.com"
                                       FontSize="12"
                                       Foreground="White"
                                       Opacity="0.8"/>
                            
                            <!-- Teléfono dinámico (sin icono, alineado como tarjeta) -->
                            <TextBlock x:Name="TxtUserPhone"
                                       Text=""
                                       FontSize="12" 
                                       Foreground="White" 
                                       Opacity="0.8"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                
                <!-- BADGE ESTADO (compacto) -->
                <Border x:Name="BadgeEstado"
                        Grid.Column="2"
                        Background="{ThemeResource AppSuccessBrush}"
                        CornerRadius="3"
                        Padding="8,3"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="TxtEstadoParte" 
                               Text="Abierto"
                               FontSize="10"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </Border>
            </Grid>
        </Border>

        <!-- ===================== CONTENT - SCROLLVIEWER CON 3 CARDS ===================== -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      Padding="0,6,0,6">
            <Grid MaxWidth="1400" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>   <!-- Card 1: Datos Generales -->
                    <RowDefinition Height="Auto"/>   <!-- Card 2: Información de Tiempo -->
                    <RowDefinition Height="*"/>      <!-- Card 3: Descripción/Acción (estirable) -->
                </Grid.RowDefinitions>
                
                
                <!-- ========== CARD 1: DATOS GENERALES ========== -->
                <Border Grid.Row="0"
                        Background="{ThemeResource AppElevatedSurfaceBrush}"
                        BorderBrush="{ThemeResource AppBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12,10">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>   <!-- Título sección -->
                            <RowDefinition Height="Auto"/>   <!-- Campos -->
                        </Grid.RowDefinitions>
                        
                        <!-- // Título Sección COMPACTO -->
                        <TextBlock Grid.Row="0"
                                   Text="DATOS GENERALES" 
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource AppPrimaryBrush}"
                                   Margin="0,0,0,4"/>
                        
                        <!-- // Grid responsive para campos (3 cols max, adaptable) -->
                        <Grid x:Name="Card1Grid" 
                              Grid.Row="1"
                              ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>  <!-- Fecha -->
                                <ColumnDefinition Width="*"/>    <!-- Cliente -->
                                <ColumnDefinition Width="220"/>  <!-- Tienda -->
                            </Grid.ColumnDefinitions>
                            
                            <!-- CAMPO: Fecha (Columna 0, Row 0) -->
                            <StackPanel x:Name="FechaStack" Grid.Column="0" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Fecha" Style="{StaticResource FieldLabel}"/>
                                <CalendarDatePicker x:Name="DpFecha"
                                                  Style="{StaticResource ModernDatePicker}"
                                                  DateFormat="{}{day.integer}/{month.integer}/{year.full}"
                                                  TabIndex="0"/>
                            </StackPanel>
                            
                            <!-- CAMPO: Cliente (Columna 1, Row 0) -->
                            <StackPanel x:Name="ClienteStack" Grid.Column="1" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Cliente" 
                                           Style="{StaticResource FieldLabel}"/>
                                
                                <!-- Grid con AutoSuggestBox + Icono de nota -->
                                <Grid ColumnSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <AutoSuggestBox x:Name="TxtCliente"
                                                  Grid.Column="0"
                                                  Style="{StaticResource ModernAutoSuggestBox}"
                                                  PlaceholderText="Buscar o crear cliente..."
                                                  TextChanged="OnClienteTextChanged"
                                                  SuggestionChosen="OnClienteSuggestionChosen"
                                                  QuerySubmitted="OnClienteQuerySubmitted"
                                                  TabIndex="1"/>
                                    
                                    <!-- Icono de nota (📝) solo visible si hay nota -->
                                    <Button x:Name="BtnClienteNota"
                                            Grid.Column="1"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Padding="6"
                                            Width="32"
                                            Height="32"
                                            VerticalAlignment="Center"
                                            Click="OnClienteNotaClick">
                                        <TextBlock x:Name="TxtNotaIcono"
                                                   Text="📝"
                                                   FontSize="16"/>
                                        <ToolTipService.ToolTip>
                                            <ToolTip x:Name="ClienteNotaTooltip">
                                                <TextBlock x:Name="TxtNotaTooltip" 
                                                           Text="Sin nota"
                                                           MaxWidth="300"
                                                           TextWrapping="Wrap"/>
                                            </ToolTip>
                                        </ToolTipService.ToolTip>
                                    </Button>
                                </Grid>
                            </StackPanel>
                            
                            <!-- CAMPO: Tienda (Columna 2, Row 0) -->
                            <StackPanel x:Name="TiendaStack" Grid.Column="2" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Tienda" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TxtTienda"
                                       Style="{StaticResource ModernTextBox}"
                                       PlaceholderText="Nombre de tienda"
                                       TabIndex="2"/>
                            </StackPanel>
                            
                        </Grid>
                    </Grid>
                </Border>
                
                <!-- ========== CARD 2: INFORMACIÓN DE TIEMPO ========== -->
                <Border Grid.Row="1"
                        Background="{ThemeResource AppElevatedSurfaceBrush}"
                        BorderBrush="{ThemeResource AppBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12,8,12,6">
                    <Grid RowSpacing="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- // Título Sección COMPACTO -->
                        <TextBlock Grid.Row="0"
                                   Text="INFORMACIÓN DE TIEMPO" 
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource AppPrimaryBrush}"
                                   Margin="0,0,0,4"/>
                        
                        <!-- // Grid responsive (5 cols max, adaptable) -->
                        <Grid x:Name="Card2Grid"
                              Grid.Row="1"
                              ColumnSpacing="12"
                              RowSpacing="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>  <!-- Hora Ini -->
                                <ColumnDefinition Width="100"/>  <!-- Hora Fin -->
                                <ColumnDefinition Width="150"/>  <!-- Ticket -->
                                <ColumnDefinition Width="*"/>    <!-- Grupo -->
                                <ColumnDefinition Width="*"/>    <!-- Tipo -->
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- CAMPO: Hora Inicio (Columna 0, Row 0) -->
                            <StackPanel x:Name="HoraIniStack" Grid.Column="0" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Hora Ini" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TxtHoraInicio"
                                       Style="{StaticResource ModernTextBox}"
                                       PlaceholderText="HH:mm"
                                       MaxLength="5"
                                       InputScope="Number"
                                       TextChanged="OnHoraTextChanged"
                                       TabIndex="3"/>
                            </StackPanel>
                            
                            <!-- CAMPO: Hora Fin (Columna 1, Row 0) -->
                            <StackPanel x:Name="HoraFinStack" Grid.Column="1" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Hora Fin" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TxtHoraFin"
                                       Style="{StaticResource ModernTextBox}"
                                       PlaceholderText="HH:mm"
                                       MaxLength="5"
                                       InputScope="Number"
                                       TextChanged="OnHoraTextChanged"
                                       TabIndex="4"/>
                            </StackPanel>
                            
                            <!-- CAMPO: Ticket (Columna 2, Row 0) -->
                            <StackPanel x:Name="TicketStack" Grid.Column="2" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Ticket" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TxtTicket"
                                       Style="{StaticResource ModernTextBox}"
                                       PlaceholderText="Número de ticket"
                                       TextChanged="OnFieldChanged"
                                       TabIndex="5"/>
                            </StackPanel>
                            
                            <!-- CAMPO: Grupo (Columna 3, Row 0) -->
                            <StackPanel x:Name="GrupoStack" Grid.Column="3" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Grupo" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="CmbGrupo"
                                        Style="{StaticResource ModernComboBox}"
                                        HorizontalAlignment="Stretch"
                                        TabIndex="6"/>
                            </StackPanel>
                            
                            <!-- CAMPO: Tipo (Columna 4, Row 0) -->
                            <StackPanel x:Name="TipoStack" Grid.Column="4" Grid.Row="0" Spacing="4">
                                <TextBlock Text="Tipo" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="CmbTipo"
                                        Style="{StaticResource ModernComboBox}"
                                        HorizontalAlignment="Stretch"
                                        TabIndex="7"/>
                            </StackPanel>

                            <StackPanel x:Name="PnlDayTimeHistory"
                                        Grid.Column="0"
                                        Grid.ColumnSpan="5"
                                        Grid.Row="1"
                                        Spacing="2"
                                        Margin="0,6,0,0"
                                        IsTabStop="False">
                                <Grid>
                                    <ProgressRing x:Name="RingDayTimeHistory"
                                                  Width="16"
                                                  Height="16"
                                                  IsActive="False"
                                                  Visibility="Collapsed"
                                                  HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="TxtDayTimeHistoryStatus"
                                               Text="No hay otros partes registrados para esta fecha."
                                               FontSize="12"
                                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                                               TextWrapping="Wrap"/>
                                </Grid>
                                <ScrollViewer x:Name="ScrDayTimeHistory"
                                              HorizontalScrollBarVisibility="Auto"
                                              VerticalScrollBarVisibility="Disabled"
                                              Visibility="Collapsed"
                                              IsTabStop="False">
                                    <Border Background="{ThemeResource AppSurfaceVariantBrush}"
                                            BorderBrush="{ThemeResource AppBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="3,2"
                                            SizeChanged="OnDayTimeHistorySizeChanged">
                                        <ItemsControl x:Name="IcDayTimeHistory"
                                                      IsTabStop="False">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Spacing="4"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="{Binding DisplayWidth}"
                                                            Height="22"
                                                            Background="{Binding Background}"
                                                            CornerRadius="4"
                                                            Padding="5,0"
                                                            ToolTipService.ToolTip="{Binding TooltipText}"
                                                            AutomationProperties.Name="{Binding AutomationName}"
                                                            IsTabStop="False">
                                                        <StackPanel Orientation="Horizontal"
                                                                    VerticalAlignment="Center"
                                                                    Spacing="4">
                                                            <TextBlock Text="{Binding OverlapGlyph}"
                                                                       FontSize="11"
                                                                       Foreground="{Binding Foreground}"/>
                                                            <TextBlock Text="{Binding TimeText}"
                                                                       FontSize="11"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{Binding Foreground}"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                            <TextBlock Text="{Binding StatusLabel}"
                                                                       FontSize="9"
                                                                       FontWeight="SemiBold"
                                                                       Opacity="0.95"
                                                                       Foreground="{Binding Foreground}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </ScrollViewer>
                            </StackPanel>

                        </Grid>
                    </Grid>
                </Border>
                
                <!-- ========== CARD 3: DESCRIPCIÓN / ACCIÓN (ESTIRABLE) ========== -->
                <Border Grid.Row="2"
                        Background="{ThemeResource AppElevatedSurfaceBrush}"
                        BorderBrush="{ThemeResource AppBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12,10"
                        VerticalAlignment="Stretch">
                    <Grid RowSpacing="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>    <!-- Título -->
                            <RowDefinition Height="*"/>       <!-- TextBox estirable -->
                        </Grid.RowDefinitions>
                        
                        <!-- // Título Sección COMPACTO -->
                        <TextBlock Grid.Row="0"
                                   Text="DESCRIPCIÓN / ACCIÓN" 
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource AppPrimaryBrush}"
                                   Margin="0,0,0,2"/>
                        
                        <!-- CAMPO: Acción/Descripción (ESTIRABLE - OCUPA TODO EL ESPACIO) -->
                        <TextBox x:Name="TxtAccion"
                                 Grid.Row="1"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalAlignment="Stretch"
                                Background="{ThemeResource AppInputBackgroundBrush}"
                                Foreground="{ThemeResource AppTextPrimaryBrush}"
                                BorderBrush="{ThemeResource AppBorderBrush}"
                                 BorderThickness="1"
                                 CornerRadius="6"
                                 Padding="12,8"
                                 FontSize="14"
                                 MinHeight="120"
                                 PlaceholderText="Describe la acción realizada o el problema atendido..."
                                 PreviewKeyDown="OnAccionPreviewKeyDown"
                                 TextChanging="OnAccionTextChanging"
                                 GotFocus="OnAccionGotFocus"
                                 TabIndex="8"/>
                        
                    </Grid>
                </Border>
                
            </Grid>
        </ScrollViewer>

        <!-- ===================== FOOTER - TAGS + BOTONERA EN LA MISMA FILA ===================== -->
        <Border Grid.Row="2" 
                Background="{ThemeResource AppSurfaceVariantBrush}"
                Padding="12,10">
            
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>      <!-- Tags (EXPANDIDO - rellena espacio) -->
                    <ColumnDefinition Width="Auto"/>   <!-- Botones (tamaño fijo) -->
                </Grid.ColumnDefinitions>
                
                <!-- ========== COLUMNA IZQUIERDA: TAGS (EXPANDIDO) ========== -->
                <Border Grid.Column="0"
                Background="{ThemeResource AppElevatedSurfaceBrush}"
                BorderBrush="{ThemeResource AppBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10,8"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Stretch">
                    <Grid RowSpacing="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>   <!-- Título + Contador -->
                            <RowDefinition Height="Auto"/>   <!-- AutoSuggestBox -->
                            <RowDefinition Height="Auto"/>   <!-- Chips -->
                        </Grid.RowDefinitions>
                        
                        <!-- Título con contador -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                       Text="TAGS / ETIQUETAS" 
                                       FontSize="9"
                                       FontWeight="SemiBold"
                        Foreground="{ThemeResource AppPrimaryBrush}"
                                       VerticalAlignment="Center"/>
                            
                            <TextBlock x:Name="TxtTagCounter"
                                       Grid.Column="1"
                                       Text="(0/5)"
                                       FontSize="9"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- AutoSuggestBox para agregar tags (EXPANDIDO) -->
                        <AutoSuggestBox x:Name="TxtTagInput"
                                        Grid.Row="1"
                                        Style="{StaticResource ModernAutoSuggestBox}"
                                        PlaceholderText="Buscar o crear tag..."
                                        QuerySubmitted="OnTagQuerySubmitted"
                                        TextChanged="OnTagTextChanged"
                                        SuggestionChosen="OnTagSuggestionChosen"
                                        Height="32"
                                        FontSize="12"
                                        HorizontalAlignment="Stretch"
                                        TabIndex="8"/>
                        
                        <!-- Chips de tags (WRAP horizontal) -->
                        <ItemsControl x:Name="TagsItemsControl" 
                                      Grid.Row="2"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" 
                                                Spacing="4"
                                                HorizontalAlignment="Left"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1A8B5CF6"
                                            BorderBrush="#8B5CF6"
                                            BorderThickness="1"
                                            CornerRadius="10"
                                            Padding="8,3">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <TextBlock Text="{Binding}"
                                                       FontSize="10"
                                                       Foreground="#E9D5FF"
                                                       VerticalAlignment="Center"/>
                                            <Button Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0"
                                                    Width="14"
                                                    Height="14"
                                                    Click="OnRemoveTagClick"
                                                    Tag="{Binding}">
                                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                                          Glyph="&#xE711;"
                                                          FontSize="9"
                                                          Foreground="#E9D5FF"/>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
                
                <!-- ========== COLUMNA DERECHA: BOTONERA ========== -->
                <StackPanel Grid.Column="1"
                            x:Name="FooterButtons"
                            Orientation="Horizontal" 
                        Spacing="12"
                        HorizontalAlignment="Right">
                
                <!-- BOTÓN: Guardar (estilo ToolbarButton) -->
                <Button x:Name="BtnGuardar"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource AppPrimaryBrush}"
                        Width="80"
                        Height="70"
                        Padding="8"
                        CornerRadius="6"
                        Click="OnGuardarClick"
                        PointerEntered="OnButtonPointerEntered"
                        PointerExited="OnButtonPointerExited"
                        TabIndex="9">
                    <StackPanel Spacing="4">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74E;" FontSize="24" Foreground="#10B981"/>
                            <TextBlock Text="Guardar" FontSize="11" Foreground="{ThemeResource AppTextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- BOTÓN: Guardar y Cerrar (estilo ToolbarButton más ancho) -->
                <Button x:Name="BtnGuardarYCerrar"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource AppPrimaryBrush}"
                        Width="110"
                        Height="70"
                        Padding="6"
                        CornerRadius="6"
                        Click="OnGuardarYCerrarClick"
                        PointerEntered="OnButtonPointerEntered"
                        PointerExited="OnButtonPointerExited"
                        TabIndex="10">
                    <StackPanel Spacing="4">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74E;" FontSize="24" Foreground="#10B981"/>
                            <TextBlock Text="Guardar y Cerrar" FontSize="10" Foreground="{ThemeResource AppTextPrimaryBrush}" HorizontalAlignment="Center" TextWrapping="NoWrap"/>
                    </StackPanel>
                </Button>
                
                <!-- BOTÓN: Anular (estilo ToolbarButton) -->
                <Button x:Name="BtnCancelar"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource AppPrimaryBrush}"
                        Width="80"
                        Height="70"
                        Padding="8"
                        CornerRadius="6"
                        Click="OnCancelarClick"
                        PointerEntered="OnButtonPointerEntered"
                        PointerExited="OnButtonPointerExited"
                        TabIndex="11">
                    <StackPanel Spacing="4">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="24" Foreground="#EF4444"/>
                            <TextBlock Text="Anular" FontSize="11" Foreground="{ThemeResource AppTextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- BOTÓN: Salir (estilo ToolbarButton) -->
                <Button x:Name="BtnSalir"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource AppPrimaryBrush}"
                        Width="80"
                        Height="70"
                        Padding="8"
                        CornerRadius="6"
                        Click="OnSalirClick"
                        PointerEntered="OnButtonPointerEntered"
                        PointerExited="OnButtonPointerExited"
                        TabIndex="12">
                    <StackPanel Spacing="4">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8BB;" FontSize="24" Foreground="#F59E0B"/>
                            <TextBlock Text="Salir" FontSize="11" Foreground="{ThemeResource AppTextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
            </StackPanel>
            </Grid>
        </Border>

        <!-- CAMPOS OCULTOS (compatibilidad) -->
        <StackPanel Grid.Row="2" 
                    Grid.RowSpan="1"
                    Visibility="Collapsed">
            <TextBox x:Name="TxtDuracion" IsTabStop="False"/>
            <TextBox x:Name="TxtGrupo" IsTabStop="False"/>
            <TextBox x:Name="TxtTipo" IsTabStop="False"/>
            <TextBox x:Name="TxtTecnico" IsTabStop="False"/>
            <TextBox x:Name="TxtEstado" IsTabStop="False"/>
            <Button x:Name="BtnAccionGrabar" IsTabStop="False"/>
        </StackPanel>
        
        <!-- ===================== CONTENTDIALOG: NOTAS CLIENTE (global + personal) ===================== -->
        <ContentDialog x:Name="ClienteNotaDialog"
                       Title="Notas del Cliente"
                       CloseButtonText="Cerrar"
                       DefaultButton="Close">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="480">
                <StackPanel Spacing="16" MinWidth="420">

                    <!-- GT-BEGIN: Nota global del cliente -->
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE774;" FontSize="14" Foreground="{ThemeResource AppPrimaryBrush}"/>
                            <TextBlock Text="Nota del cliente (global)"
                                       FontSize="14" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtNotaGlobalMeta"
                                   FontSize="11"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="TxtNotaGlobalEditor"
                                 PlaceholderText="Nota global del cliente..."
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="140"
                                 VerticalContentAlignment="Top"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource ModernTextBox}"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="BtnSaveNotaGlobal"
                                    Content="Guardar nota global"
                                    Click="OnSaveNotaGlobal_Click"
                        Background="{ThemeResource AppPrimaryBrush}"
                                    Foreground="White"
                                    Padding="12,6"
                                    CornerRadius="6"
                                    FontSize="13"/>
                            <TextBlock x:Name="TxtNotaGlobalStatus"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                    <!-- GT-END -->

                <Border Height="1" Background="{ThemeResource AppBorderBrush}" Margin="0,4"/>

                    <!-- GT-BEGIN: Nota personal del usuario -->
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE77B;" FontSize="14" Foreground="{ThemeResource AppPrimaryBrush}"/>
                            <TextBlock Text="Mi nota personal"
                                       FontSize="14" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtNotaPersonalMeta"
                                   FontSize="11"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="TxtNotaPersonalEditor"
                                 PlaceholderText="Tu nota personal para este cliente..."
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="140"
                                 VerticalContentAlignment="Top"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource ModernTextBox}"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="BtnSaveNotaPersonal"
                                    Content="Guardar mi nota"
                                    Click="OnSaveNotaPersonal_Click"
                        Background="{ThemeResource AppPrimaryBrush}"
                                    Foreground="White"
                                    Padding="12,6"
                                    CornerRadius="6"
                                    FontSize="13"/>
                            <TextBlock x:Name="TxtNotaPersonalStatus"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                    <!-- GT-END -->

                    <!-- GT-BEGIN: Indicador de carga -->
                    <StackPanel x:Name="PnlNotasLoading"
                                Orientation="Horizontal" Spacing="8"
                                HorizontalAlignment="Center"
                                Visibility="Collapsed">
                        <ProgressRing IsActive="True" Width="16" Height="16"/>
                        <TextBlock Text="Cargando notas..." FontSize="12"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"/>
                    </StackPanel>
                    <!-- GT-END -->

                </StackPanel>
            </ScrollViewer>
        </ContentDialog>
    </Grid>
</Page>

