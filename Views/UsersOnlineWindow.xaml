<Window
    x:Class="GestionTime.Desktop.Views.UsersOnlineWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Usuarios Online">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#0FA7B6" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Usuarios Online" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock x:Name="TxtSubtitle" Text="Actualizando..." FontSize="13" Foreground="#E0F7FA" Margin="0,4,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        x:Name="BtnRefresh"
                        Click="OnRefreshClick"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="12"
                        VerticalAlignment="Center"
                        ToolTipService.ToolTip="Actualizar lista de usuarios">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon x:Name="RefreshIcon" 
                                  Glyph="&#xE72C;" 
                                  FontSize="16" 
                                  Foreground="White">
                            <FontIcon.RenderTransform>
                                <RotateTransform CenterX="8" CenterY="8"/>
                            </FontIcon.RenderTransform>
                        </FontIcon>
                        <TextBlock Text="Actualizar" 
                                   FontSize="13" 
                                   Foreground="White" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.Resources>
                        <Storyboard x:Name="RefreshAnimation">
                            <DoubleAnimation Storyboard.TargetName="RefreshIcon"
                                           Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                           From="0" To="360"
                                           Duration="0:0:0.6"
                                           RepeatBehavior="1x"/>
                        </Storyboard>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Padding="12">
            <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                <ProgressRing IsActive="True" Width="48" Height="48" Foreground="#0FA7B6"/>
                <TextBlock Text="Cargando usuarios..." Margin="0,12,0,0" FontSize="14" Foreground="#666666" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel x:Name="ErrorPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" Padding="24">
                <TextBlock x:Name="TxtError" Text="Error al cargar usuarios" Margin="0,12,0,0" FontSize="14" Foreground="#666666" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="300"/>
            </StackPanel>
            <ScrollViewer x:Name="UsersScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="UsersListView" Margin="0,8">
                    <!-- 🆕 NUEVO: Agrupación por Rol -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Encabezado de Grupo -->
                            <StackPanel Margin="0,0,0,8">
                                <TextBlock Text="{Binding GroupName}" 
                                           FontSize="13" 
                                           FontWeight="SemiBold" 
                                           Foreground="#666666" 
                                           Margin="4,12,0,8"/>
                                
                                <!-- Usuarios del Grupo -->
                                <ItemsControl ItemsSource="{Binding Users}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F5F5F5" CornerRadius="8" Padding="16,12" Margin="0,0,0,8" BorderBrush="#E0E0E0" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <StackPanel Grid.Column="0">
                                                        <!-- ✅ CAMBIO 3: Nombre con ellipsis -->
                                                        <TextBlock Text="{Binding FullName}" 
                                                                   FontSize="15" 
                                                                   FontWeight="SemiBold" 
                                                                   Foreground="#333333"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxLines="1"/>
                                                        
                                                        <!-- ✅ CAMBIO 3: Email con ellipsis -->
                                                        <TextBlock Text="{Binding Email}" 
                                                                   FontSize="12" 
                                                                   Foreground="#666666" 
                                                                   Margin="0,2,0,0"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxLines="1"/>
                                                        
                                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                            <!-- Badge de Rol -->
                                                            <Border Background="#E3F2FD" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                                                <TextBlock Text="{Binding RoleBadge}" FontSize="11" Foreground="#1976D2" FontWeight="SemiBold"/>
                                                            </Border>
                                                            
                                                            <!-- ✅ CAMBIO 1: Badge Premium Online/Offline -->
                                                            <Border x:Name="StatusBadge"
                                                                    CornerRadius="10" 
                                                                    Padding="8,3"
                                                                    Background="{Binding StatusBadgeBackground}">
                                                                <StackPanel Orientation="Horizontal" Spacing="4">
                                                                    <Ellipse Width="6" Height="6" Fill="{Binding StatusBadgeDotColor}" VerticalAlignment="Center"/>
                                                                    <TextBlock Text="{Binding StatusText}" 
                                                                               FontSize="11" 
                                                                               Foreground="{Binding StatusBadgeTextColor}"
                                                                               FontWeight="SemiBold"
                                                                               VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
