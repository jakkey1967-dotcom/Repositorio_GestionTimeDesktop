<Page
    x:Class="GestionTime.Desktop.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    RequestedTheme="Dark">
    
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                
                <!-- TEMA OSCURO -->
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="PageBackground" Color="#1a1d21"/>
                    <SolidColorBrush x:Key="CardBackground" Color="#2c3e50"/>
                    <SolidColorBrush x:Key="InputBackground" Color="#1f2937"/>
                    <SolidColorBrush x:Key="InputForeground" Color="#f3f4f6"/>
                    <SolidColorBrush x:Key="TitleForeground" Color="#e5e7eb"/>
                    <SolidColorBrush x:Key="LabelForeground" Color="#9ca3af"/>
                    <SolidColorBrush x:Key="TealPrimary" Color="#098ca3"/>
                    <SolidColorBrush x:Key="TealDark" Color="#076b7d"/>
                    <SolidColorBrush x:Key="TealLight" Color="#0bb3cf"/>
                    <ImageBrush x:Key="BackgroundImage" 
                                ImageSource="ms-appx:///Assets/login_fondoOscuro.png" 
                                Stretch="UniformToFill"
                                AlignmentX="Center"
                                AlignmentY="Center"/>
                </ResourceDictionary>
                
                <!-- TEMA CLARO -->
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="PageBackground" Color="#f3f4f6"/>
                    <SolidColorBrush x:Key="CardBackground" Color="#ffffff"/>
                    <SolidColorBrush x:Key="InputBackground" Color="#ffffff"/>
                    <SolidColorBrush x:Key="InputForeground" Color="#1f2937"/>
                    <SolidColorBrush x:Key="TitleForeground" Color="#1a3b47"/>
                    <SolidColorBrush x:Key="LabelForeground" Color="#6b7280"/>
                    <SolidColorBrush x:Key="TealPrimary" Color="#098ca3"/>
                    <SolidColorBrush x:Key="TealDark" Color="#076b7d"/>
                    <SolidColorBrush x:Key="TealLight" Color="#0bb3cf"/>
                    <ImageBrush x:Key="BackgroundImage" 
                                ImageSource="ms-appx:///Assets/login_fondoClaro.png" 
                                Stretch="UniformToFill"
                                AlignmentX="Center"
                                AlignmentY="Center"/>
                </ResourceDictionary>
                
            </ResourceDictionary.ThemeDictionaries>

            <!-- Estilo TextBox moderno con tema -->
            <Style x:Key="ModernTextBox" TargetType="TextBox">
                <Setter Property="Background" Value="{ThemeResource InputBackground}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                <Setter Property="BorderBrush" Value="{ThemeResource TealPrimary}"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{ThemeResource InputForeground}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealLight}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Focused">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealLight}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderThickness">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="0,0,0,3"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#3f3f46"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border x:Name="BorderElement"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ScrollViewer x:Name="ContentElement"
                                                Padding="{TemplateBinding Padding}"
                                                HorizontalScrollMode="Auto"
                                                HorizontalScrollBarVisibility="Hidden"
                                                VerticalScrollMode="Auto"
                                                VerticalScrollBarVisibility="Hidden"
                                                IsTabStop="False"
                                                ZoomMode="Disabled"
                                                AutomationProperties.AccessibilityView="Raw"/>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo PasswordBox moderno con tema -->
            <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
                <Setter Property="Background" Value="{ThemeResource InputBackground}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                <Setter Property="BorderBrush" Value="{ThemeResource TealPrimary}"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{ThemeResource InputForeground}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="PasswordBox">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealLight}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Focused">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealLight}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="BorderThickness">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="0,0,0,3"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" 
                                                                              Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#3f3f46"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border x:Name="BorderElement"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ScrollViewer x:Name="ContentElement"
                                                Padding="{TemplateBinding Padding}"
                                                HorizontalScrollMode="Disabled"
                                                HorizontalScrollBarVisibility="Disabled"
                                                VerticalScrollMode="Disabled"
                                                VerticalScrollBarVisibility="Disabled"
                                                IsTabStop="False"
                                                ZoomMode="Disabled"
                                                AutomationProperties.AccessibilityView="Raw"/>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo Botón principal -->
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource TealPrimary}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="CornerRadius" Value="25"/>
                <Setter Property="Padding" Value="40,12"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid" 
                                  Background="{TemplateBinding Background}" 
                                  CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" 
                                                                              Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealDark}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" 
                                                                              Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TealLight}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" 
                                                                              Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#6b7280"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" 
                                                                              Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter x:Name="ContentPresenter"
                                                Content="{TemplateBinding Content}"
                                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Padding="{TemplateBinding Padding}"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                AutomationProperties.AccessibilityView="Raw"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo HyperlinkButton -->
            <Style x:Key="LinkButton" TargetType="HyperlinkButton">
                <Setter Property="Foreground" Value="{ThemeResource TealPrimary}"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="PageRootGrid" Opacity="0">
        <!-- Fondo con imagen dinámica según tema -->
        <Grid.Background>
            <ThemeResource ResourceKey="BackgroundImage"/>
        </Grid.Background>

        <!-- Selector de tema (esquina superior izquierda) -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20"
                    Spacing="8">
            <Button x:Name="BtnTheme"
                    Width="40"
                    Height="40"
                    CornerRadius="20"
                    Background="{ThemeResource CardBackground}"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource TealPrimary}"
                    ToolTipService.ToolTip="Tema">
                <FontIcon FontFamily="Segoe MDL2 Assets" 
                          Glyph="&#xE700;" 
                          FontSize="18"
                          Foreground="{ThemeResource TealPrimary}"/>
                <Button.Flyout>
                    <MenuFlyout>
                        <ToggleMenuFlyoutItem x:Name="ThemeSystemItem" 
                                              Text="Tema del sistema" 
                                              Click="OnThemeSystem"/>
                        <ToggleMenuFlyoutItem x:Name="ThemeLightItem" 
                                              Text="Tema claro" 
                                              Click="OnThemeLight"/>
                        <ToggleMenuFlyoutItem x:Name="ThemeDarkItem" 
                                              Text="Tema oscuro" 
                                              Click="OnThemeDark"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </StackPanel>

        <!-- Card del formulario -->
        <Border x:Name="LoginCard"
                Width="290" 
                CornerRadius="20"
                Background="{ThemeResource CardBackground}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="50,50,50,50"
                Padding="10,10">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <StackPanel VerticalAlignment="Top"
                        Spacing="8">

                <!-- Título -->
                <TextBlock Text="Inicio de Sesión"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{ThemeResource TitleForeground}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>

                <!-- Correo electrónico -->
                <StackPanel Spacing="5">
                    <TextBlock Text="Correo electrónico"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource LabelForeground}"/>
                    <Grid>
                        <TextBox x:Name="TxtUser"
                                 Style="{StaticResource ModernTextBox}"
                                 PlaceholderText="correo@dominio.com"
                                 Height="36"/>
                        <FontIcon HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"
                                  Glyph="&#xE77B;"
                                  FontSize="16"
                                  Foreground="{ThemeResource TealPrimary}"/>
                    </Grid>
                </StackPanel>

                <!-- Contraseña -->
                <StackPanel Spacing="5">
                    <TextBlock Text="Contraseña"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource LabelForeground}"/>
                    <Grid>
                        <!-- PasswordBox (visible por defecto) -->
                        <PasswordBox x:Name="TxtPass"
                                     Style="{StaticResource ModernPasswordBox}"
                                     PlaceholderText="••••••••"
                                     Height="36"
                                     Visibility="Visible"
                                     KeyDown="OnPasswordKeyDown"/>
                        
                        <!-- TextBox para mostrar contraseña (oculto por defecto) -->
                        <TextBox x:Name="TxtPassVisible"
                                 Style="{StaticResource ModernTextBox}"
                                 PlaceholderText="••••••••"
                                 Height="36"
                                 Visibility="Collapsed"
                                 KeyDown="OnPasswordKeyDown"/>
                        
                        <!-- Botón para mostrar/ocultar contraseña -->
                        <Button x:Name="BtnTogglePassword"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,8,0"
                                Width="36"
                                Height="36"
                                Background="Transparent"
                                BorderThickness="0"
                                Click="OnTogglePasswordClick"
                                ToolTipService.ToolTip="Mostrar/Ocultar contraseña">
                            <FontIcon x:Name="IconPassword"
                                      Glyph="&#xE7B3;"
                                      FontSize="16"
                                      Foreground="{ThemeResource TealPrimary}"/>
                        </Button>
                    </Grid>
                </StackPanel>

                <!-- Botón Iniciar sesión -->
                <Button x:Name="BtnLogin"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Center"
                        Click="OnLoginClick"
                        Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ProgressRing x:Name="Prg"
                                      Width="16" Height="16"
                                      IsActive="False"
                                      Visibility="Collapsed"
                                      Foreground="White"/>
                        <TextBlock Text="Iniciar sesión" FontSize="15"/>
                    </StackPanel>
                </Button>

                <!-- Mensaje de error/éxito - MOVIDO ANTES DEL STATUS -->
                <Border x:Name="MsgBox"
                        Visibility="Collapsed"
                        Background="#FEF3C7"
                        BorderBrush="#F59E0B"
                        BorderThickness="2"
                        CornerRadius="8"
                        Padding="10,8"
                        Margin="0,8,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <FontIcon Grid.Column="0"
                                  x:Name="MsgIcon"
                                  Glyph="&#xE7BA;"
                                  FontSize="14"
                                  Foreground="#D97706"
                                  VerticalAlignment="Top"
                                  Margin="0,1,8,0"/>
                        <TextBlock Grid.Column="1"
                                   x:Name="LblMsg"
                                   TextWrapping="Wrap"
                                   FontSize="11"
                                   LineHeight="16"
                                   Foreground="#92400E"/>
                    </Grid>
                </Border>

                <!-- Status (mensaje de "Conectando...") -->
                <TextBlock x:Name="TxtStatus"
                           Text=""
                           Foreground="{ThemeResource LabelForeground}"
                           FontSize="10"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,4,0,0"
                           Visibility="Collapsed"/>

                <!-- Enlaces -->
                <StackPanel Spacing="2"
                            HorizontalAlignment="Center"
                            Margin="0,4,0,0">
                    <HyperlinkButton Content="¿Olvidó su contraseña?"
                                     Click="OnForgotPasswordClick"
                                     Style="{StaticResource LinkButton}"
                                     HorizontalAlignment="Center"
                                     FontSize="11"
                                     Padding="0,1"/>
                    <HyperlinkButton Content="Registrarse como nuevo usuario"
                                     x:Name="BtnRegister"
                                     Click="OnRegisterClick"
                                     Style="{StaticResource LinkButton}"
                                     HorizontalAlignment="Center"
                                     FontWeight="SemiBold"
                                     FontSize="11"
                                     Padding="0,1"/>
                </StackPanel>

                <!-- Recordar sesión -->
                <CheckBox x:Name="ChkRemember"
                          Content="Recordar sesión"
                          Foreground="{ThemeResource LabelForeground}"
                          FontSize="10"
                          HorizontalAlignment="Center"
                          Margin="0,2,0,0"/>

                <!-- Link Ayuda -->
                <HyperlinkButton Content="Ayuda"
                                 Foreground="{ThemeResource TealPrimary}"
                                 FontSize="10"
                                 HorizontalAlignment="Center"
                                 Padding="0,1"
                                 Margin="0,2,0,0"/>

            </StackPanel>
        </Border>
    </Grid>
</Page>














